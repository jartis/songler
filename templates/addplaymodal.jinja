    <div class="modal" id="addPlayModal" data-target="#addPlayModal" tabindex="-1" role="dialog">
        <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id='modal-title'>Add 'Play' For Song</h5>
                    <button type="button" class="close" data-bs-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    <form id='addplayform'>
                        <div class="form-group">
                            <label for="addplaydate">Date</label>
                            <input type="date" class="form-control" id="addplaydate" autocomplete="off" placeholder="">
                        </div>
                    </form>
                </div>
                <input type="hidden" name="playslid" id="playslid">
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                    <button type="button" onclick="savePlay(this)" class="btn btn-primary">Add Play</button>
                </div>
            </div>
        </div>
    </div>